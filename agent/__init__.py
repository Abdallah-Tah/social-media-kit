"""Social Media Agent — an orchestrated, provider-agnostic content agent.

Drives the full routine (research → write → publish) as an autonomous
tool-using loop, powered by Claude or any OpenAI-compatible model.
"""

__version__ = "2.3.0"
